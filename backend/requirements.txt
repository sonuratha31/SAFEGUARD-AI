fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.3.0
sqlalchemy==2.0.30
httpx==0.27.0
python-dotenv==1.0.1
scikit-learn==1.5.0
numpy==1.26.4
PyPDF2==3.0.1
python-docx==1.1.2
pandas==2.2.2

# IBM WatsonX — optional, enabled only when credentials are configured
# ibm-watsonx-ai>=1.0.0

# ChromaDB and sentence-transformers intentionally omitted for Vercel.
# The application automatically uses the TF-IDF RAG fallback.
